# `pibt.rs`


This crate contains an extremely simple implementation of PiBT and two versions for
the heterogenous version of PiBT. The first version is using a reasoning module within the winPiBT
extension. The second is using CBS as a protocol